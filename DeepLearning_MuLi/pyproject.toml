[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "d2l-learning"
version = "0.1.0"

# 关键：明确指定包的位置
[tool.setuptools]
packages = {find = {where = ["."]}}  # 在当前目录查找包

[tool.setuptools.package-dir]
# 如果你想要不同的包名映射，可以在这里配置